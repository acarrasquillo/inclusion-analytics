{% extends 'base.html.j2' %}

{% block main_header %}
	<h1 class="h2">{% block title %}Programs networks{% endblock %}</h1>
{% endblock %}

{% block content %}

	{# form add a program network #}
	<form method="post">
	  <div class="mb-3">
	    <label for="name" class="form-label">Program Network name</label>
	    <input name="name" class="form-control" id="name" aria-describedby="nameHelp" required>
	    <div id="nameHelp" class="form-text">Enter the name of the program network</div>
	  </div>
	  <button type="submit" class="btn btn-primary" value="Add">Add Network</button>
	</form>


	    {% if rows|length > 0 %}
	{# dashboard content #}
	{% set columns = rows[0].keys() %}
      <div class="table-responsive">
        <table class="table table-striped table-sm">
          <thead>
            <tr>
              {% for k in columns %}
			  	<th>{{ k }}</th>
			  {% endfor %}
            </tr>
          </thead>
          <tbody>
          	{% for row in rows %}
            <tr>
                {% for k in columns %}
                <td>{{ row[k] }}</td>
            	{% endfor %}
              
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
      {% endif %}

{% endblock %}